#include <stdio.h>

int main (void){

    int soma, quantidade, idade;

    float media;

    soma = 0;
    quantidade = 0;
    idade = 0;

    while (idade >= 0) {

    quantidade + 1;

    scanf("%d", &idade);

    if (idade >= 0){

        soma = soma + idade;
        quantidade = quantidade + 1;

        }
    }

    if (quantidade >= 0){

    media = (float) soma / quantidade;

    printf("%.2f\n",media);

    }

    return 0;

}
